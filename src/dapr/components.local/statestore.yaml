apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: statestore
spec:
  type: state.postgresql
  version: v1
  metadata:
    - name: connectionString
      value: "host=localhost user=dapr password=dapr port=5432 dbname=dapr_state sslmode=disable"
    - name: actorStateStore
      value: "true"
